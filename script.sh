git remote add origin https://github.com/diasrodri/diasrodri.github.io.git
git add .
git status
git commit -m "Arquivos iniciais"

git config --global user.email "diasrodri@gmail.com"
git config --global user.name "radias"

git push origin master
